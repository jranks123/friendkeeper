import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    marginBottom: 40,
    textAlign:'center'
  },
  datePicker: {
    width: 200,
    marginLeft: 10,
    marginTop: 10
  }
});
