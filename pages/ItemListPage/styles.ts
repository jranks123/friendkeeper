import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontSize: 30,
    marginTop: 0
  },
    noFriendsCopy: {
        marginTop: 20,
        fontSize: 18,
      color: "grey"
    }
});
