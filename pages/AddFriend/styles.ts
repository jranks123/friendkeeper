import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    buttonContainer: {
        marginTop: 20,
        flexDirection: 'row',
        justifyContent: 'flex-start'
    },
    input: {
        marginBottom: 40,
    },
    label: {
        fontWeight: 'bold',
        fontSize: 16,
        color: 'grey',
        marginLeft: 10
    },
    datePicker: {
        width: 200,
        marginLeft: 10,
        marginTop: 10
    }
});

