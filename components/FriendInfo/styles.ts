import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    buttonContainer: {
        margin: 20,
        flexDirection: 'row',
        justifyContent: 'flex-start'
    },
    input: {
        marginBottom: 40,
    }
});

